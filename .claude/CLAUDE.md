# Claude Code — Project Instructions

**Read `AGENTS.md` before doing anything else.** All project rules, workflows, skills routing, Docker rules, and architecture context live there.

This file exists only so Claude Code auto-loads it. The single source of truth is `AGENT.md`.
