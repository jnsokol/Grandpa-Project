# Claude Code — Project Instructions

**Read `dashboard-plan.md` before doing anything else.** All project rules, workflows, skills routing, Docker rules, and architecture context live there.

**ALWAYS AFTER THE CHANGES ADD EVERYTHING BY 'git add .' COMMAND AND COMMIT CHANGES. AFTER IT 'git push'!**
This file exists only so Claude Code auto-loads it. The single source of truth is `AGENT.md`.
